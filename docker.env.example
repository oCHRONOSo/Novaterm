# ================================
# Novaterm Docker Environment
# Copy to .env and customize
# ================================

# Database Configuration
DB_ROOT_PASSWORD=rootpassword
DB_NAME=novaterm
DB_USER=novaterm
DB_PASSWORD=novaterm123
DB_PORT=3306

# Application Configuration
APP_PORT=3000

# Security (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=change-this-to-a-secure-secret-in-production
ENCRYPTION_SECRET=your-encryption-secret-key-here-min-32-chars

# Ollama AI (optional - for AI features)
# If running Ollama on host machine, use host.docker.internal
OLLAMA_HOST=http://host.docker.internal:11434

# Socket Configuration (usually don't need to change)
SOCKET_HOST=http://localhost:3000
NEXT_PUBLIC_SOCKET_HOST=http://localhost:3000

